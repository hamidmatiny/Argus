apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "stream-processor.serviceAccountName" . }}
  labels:
    {{- include "stream-processor.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
