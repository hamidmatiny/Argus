apiVersion: v1
kind: Service
metadata:
  name: {{ include "stream-processor.fullname" . }}
  labels:
    {{- include "stream-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stream-processor.selectorLabels" . | nindent 4 }}
