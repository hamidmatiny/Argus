apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stream-processor.fullname" . }}-config
  labels:
    {{- include "stream-processor.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
