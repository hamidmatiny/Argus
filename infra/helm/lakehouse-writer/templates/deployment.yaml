apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lakehouse-writer.fullname" . }}
  labels:
    {{- include "lakehouse-writer.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "lakehouse-writer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "lakehouse-writer.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: lakehouse-writer
    spec:
      serviceAccountName: {{ include "lakehouse-writer.serviceAccountName" . }}
      containers:
        - name: lakehouse-writer
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "lakehouse-writer.fullname" . }}-config
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
