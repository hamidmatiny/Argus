apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lakehouse-writer.serviceAccountName" . }}
  labels:
    {{- include "lakehouse-writer.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
