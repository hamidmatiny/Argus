apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-engine.fullname" . }}
  labels:
    {{- include "incident-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incident-engine.selectorLabels" . | nindent 4 }}
