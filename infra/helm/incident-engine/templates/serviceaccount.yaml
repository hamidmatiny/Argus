apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "incident-engine.serviceAccountName" . }}
  labels:
    {{- include "incident-engine.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
