replicaCount: 1

image:
  repository: ghcr.io/argus-platform/incident-engine
  pullPolicy: IfNotPresent
  tag: "0.1.0"

serviceAccount:
  create: true
  name: "incident-engine"
  annotations: {}


service:
  type: ClusterIP
  port: 8098

probes:
  livenessPath: /health
  readinessPath: /readyz

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

networkPolicy:
  enabled: true
  allowIngressFrom: []
  allowEgressTo:
    - api-gateway
  # VPC CIDR + AWS API prefixes filled per environment values
  egressCIDRs:
    - 10.0.0.0/8

env:
  ARGUS_ENV: "dev"
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.argus.svc:4317"
