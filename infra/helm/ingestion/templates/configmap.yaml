apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ingestion.fullname" . }}-config
  labels:
    {{- include "ingestion.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
