apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ingestion.fullname" . }}
  labels:
    {{- include "ingestion.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ingestion.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ingestion.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: ingestion
    spec:
      serviceAccountName: {{ include "ingestion.serviceAccountName" . }}
      containers:
        - name: ingestion
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "ingestion.fullname" . }}-config
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.livenessPath }}
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readinessPath }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
