apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "drift-monitor.serviceAccountName" . }}
  labels:
    {{- include "drift-monitor.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
