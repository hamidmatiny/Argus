dagster>=1.8,<1.11
dagster-webserver>=1.8,<1.11
dagster-postgres>=0.24,<0.27
# Align upper bounds with feast + mlflow (pip ResolutionTooDeep otherwise):
# feast: numpy<2, pandas<3, pyarrow<18.1; mlflow: pandas!=2.3.0,<3, pyarrow<20
pandas>=2.1,<3,!=2.3.0
numpy>=1.26,<2
pyarrow>=17,<18.1.0
pyiceberg[pyarrow,s3fs]>=0.7.1,<0.10
kafka-python>=2.0.2,<3
mlflow>=2.14,<3
psycopg2-binary>=2.9,<3
# feast has no [file] extra (aws/gcp/…); fake extras confuse the resolver.
feast>=0.40,<0.48
pytest>=8.0,<10
boto3>=1.34,<2
