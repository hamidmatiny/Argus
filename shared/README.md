# shared

Shared libraries, protobuf/OpenAPI contracts, and cross-cutting utilities used by multiple ARGUS services.

**Status:** Scaffold only — populated in Phase 1+ (schemas, common types, auth helpers).

**Future contents (planned):**
- Protocol Buffer / Avro / JSON Schema contracts for fleet telemetry
- Shared Go/Python/TypeScript packages for IDs, timestamps, and error envelopes
- Common configuration and feature-flag helpers
