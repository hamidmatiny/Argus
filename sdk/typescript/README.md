# sdk/typescript

Official **TypeScript SDK** for browser and Node clients integrating with ARGUS APIs and the dashboard.

**Status:** Scaffold only — implemented in a later phase.

**Language:** TypeScript

**Responsibilities (planned):**
- Typed REST/gRPC-web clients
- Auth token helpers compatible with api-gateway
- Packaging for npm-style distribution
