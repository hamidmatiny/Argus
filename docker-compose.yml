# ARGUS local development stack
# Phase 0: Redpanda (Kafka-API compatible).
# Phase 1: Schema Registry (built into Redpanda) + Redpanda Console.

name: argus

services:
  # ---------------------------------------------------------------------------
  # Messaging — Redpanda (Kafka API, no ZooKeeper)
  # Built-in Confluent-compatible Schema Registry on :8081 (host :18081)
  # ---------------------------------------------------------------------------
  redpanda:
    image: docker.redpanda.com/redpandadata/redpanda:v24.2.4
    container_name: argus-redpanda
    command:
      - redpanda
      - start
      - --kafka-addr internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr internal://redpanda:9092,external://localhost:19092
      - --pandaproxy-addr internal://0.0.0.0:8082,external://0.0.0.0:18082
      - --advertise-pandaproxy-addr internal://redpanda:8082,external://localhost:18082
      - --schema-registry-addr internal://0.0.0.0:8081,external://0.0.0.0:18081
      - --rpc-addr redpanda:33145
      - --advertise-rpc-addr redpanda:33145
      - --mode dev-container
      - --smp 1
      - --default-log-level=warn
    ports:
      - "19092:19092" # Kafka API (host)
      - "18081:18081" # Schema Registry (Confluent-compatible)
      - "18082:18082" # Pandaproxy (HTTP proxy)
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy.*true'"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    volumes:
      - redpanda-data:/var/lib/redpanda/data

  # ---------------------------------------------------------------------------
  # Phase 1 — Redpanda Console (UI for topics + Schema Registry subjects)
  # Schema Registry URL points at Redpanda's built-in SR (not a separate Karapace).
  # ---------------------------------------------------------------------------
  redpanda-console:
    image: docker.redpanda.com/redpandadata/console:v2.8.5
    container_name: argus-redpanda-console
    entrypoint: /bin/sh
    command: -c 'echo "$$CONSOLE_CONFIG_FILE" > /tmp/config.yml; /app/console'
    environment:
      CONFIG_FILEPATH: /tmp/config.yml
      CONSOLE_CONFIG_FILE: |
        kafka:
          brokers: ["redpanda:9092"]
          schemaRegistry:
            enabled: true
            urls: ["http://redpanda:8081"]
        redpanda:
          adminApi:
            enabled: true
            urls: ["http://redpanda:9644"]
    ports:
      - "8087:8080"
    depends_on:
      redpanda:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Phase 2 — ingestion (simulator + Ray consumer)
  # Resource limits mirror the 2 CPU / 2Gi pattern used in source fleet services.
  # ---------------------------------------------------------------------------
  simulator:
    build:
      context: .
      dockerfile: ingestion/simulator/Dockerfile
    container_name: argus-simulator
    environment:
      KAFKA_BROKERS: redpanda:9092
      SCHEMA_REGISTRY_URL: http://redpanda:8081
      SIMULATOR_TOPIC: telemetry.raw
      SIMULATOR_VEHICLES: ${SIMULATOR_VEHICLES:-5}
      SIMULATOR_RATE: ${SIMULATOR_RATE:-10}
      SIMULATOR_DURATION: ${SIMULATOR_DURATION:-0}
      SIMULATOR_FAILURE_RATE: ${SIMULATOR_FAILURE_RATE:-0.05}
      SIMULATOR_HEALTH_PORT: "8091"
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ARGUS_AVRO_SCHEMA_PATH: /app/shared/avro/telemetry_event.avsc
    ports:
      - "8091:8091"
    depends_on:
      redpanda:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8091/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 25s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.25"
          memory: 256M
    restart: unless-stopped

  ray-consumer:
    build:
      context: .
      dockerfile: ingestion/ray_consumer/Dockerfile
    container_name: argus-ray-consumer
    environment:
      KAFKA_BROKERS: redpanda:9092
      SCHEMA_REGISTRY_URL: http://redpanda:8081
      INGESTION_RAW_TOPIC: telemetry.raw
      INGESTION_NORMALIZED_TOPIC: telemetry.normalized
      INGESTION_KAFKA_GROUP_ID: argus-ingestion
      RAY_NUM_PARTITIONS: ${RAY_NUM_PARTITIONS:-4}
      RAY_NUM_CPUS: ${RAY_NUM_CPUS:-2}
      RAY_BATCH_SIZE: ${RAY_BATCH_SIZE:-50}
      RAY_HEALTH_PORT: "8092"
      RAY_DASHBOARD_HOST: "0.0.0.0"
      RAY_DASHBOARD_PORT: "8265"
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ARGUS_AVRO_SCHEMA_PATH: /app/shared/avro/telemetry_event.avsc
    ports:
      - "8092:8092"
      - "8265:8265"
    depends_on:
      redpanda:
        condition: service_healthy
      simulator:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8092/health"]
      interval: 20s
      timeout: 5s
      retries: 8
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M
    restart: unless-stopped

  # ---------------------------------------------------------------------------
  # Phase — stream-processor (Flink QA gate) (placeholder)
  # ---------------------------------------------------------------------------
  # stream-processor:
  #   build: ./stream-processor
  #   depends_on:
  #     redpanda:
  #       condition: service_healthy

  # ---------------------------------------------------------------------------
  # Phase — lakehouse (Iceberg catalog / compute deps) (placeholder)
  # ---------------------------------------------------------------------------
  # lakehouse-catalog: ...
  # lakehouse-query: ...

  # ---------------------------------------------------------------------------
  # Phase — orchestration (Dagster) (placeholder)
  # ---------------------------------------------------------------------------
  # orchestration:
  #   build: ./orchestration

  # ---------------------------------------------------------------------------
  # Phase — drift-monitor (placeholder)
  # ---------------------------------------------------------------------------
  # drift-monitor:
  #   build: ./drift-monitor

  # ---------------------------------------------------------------------------
  # Phase — incident-engine (Go) (placeholder)
  # ---------------------------------------------------------------------------
  # incident-engine:
  #   build: ./incident-engine

  # ---------------------------------------------------------------------------
  # Phase — api-gateway (Go + OPA) (placeholder)
  # ---------------------------------------------------------------------------
  # api-gateway:
  #   build: ./api-gateway

  # ---------------------------------------------------------------------------
  # Phase — observability (OTel collector, etc.) (placeholder)
  # ---------------------------------------------------------------------------
  # otel-collector:
  #   image: otel/opentelemetry-collector-contrib:latest

  # ---------------------------------------------------------------------------
  # Phase — ai-copilot (placeholder)
  # ---------------------------------------------------------------------------
  # ai-copilot:
  #   build: ./ai-copilot

  # ---------------------------------------------------------------------------
  # Phase — dashboard (placeholder)
  # ---------------------------------------------------------------------------
  # dashboard:
  #   build: ./dashboard

volumes:
  redpanda-data:
