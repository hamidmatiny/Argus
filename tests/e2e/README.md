# tests/e2e

Cross-service end-to-end tests that exercise the full ARGUS data path (ingest → QA → lakehouse → drift → incidents → API).

**Status:** Scaffold only — filled in once enough services exist to compose a path.

**Planned contents:**
- Compose-based e2e harness
- Golden-path and failure-injection scenarios
- CI gate for release candidates
